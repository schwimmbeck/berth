use colored::Colorize;

pub fn execute(client: &str) {
    println!(
        "{} {} is not yet implemented.",
        "!".yellow().bold(),
        "berth link".bold()
    );
    println!("  Would configure client: {}", client.cyan());
}
