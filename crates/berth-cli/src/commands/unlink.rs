use colored::Colorize;

pub fn execute(client: &str) {
    println!(
        "{} {} is not yet implemented.",
        "!".yellow().bold(),
        "berth unlink".bold()
    );
    println!("  Would unlink client: {}", client.cyan());
}
