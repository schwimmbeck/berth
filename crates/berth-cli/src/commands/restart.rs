use colored::Colorize;

pub fn execute(server: &str) {
    println!(
        "{} {} is not yet implemented.",
        "!".yellow().bold(),
        "berth restart".bold()
    );
    println!("  Would restart server: {}", server.cyan());
}
