use colored::Colorize;

pub fn execute(server: &str) {
    println!(
        "{} {} is not yet implemented.",
        "!".yellow().bold(),
        "berth install".bold()
    );
    println!("  Would install server: {}", server.cyan());
}
