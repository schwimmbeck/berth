use colored::Colorize;

pub fn execute(server: &str) {
    println!(
        "{} {} is not yet implemented.",
        "!".yellow().bold(),
        "berth uninstall".bold()
    );
    println!("  Would uninstall server: {}", server.cyan());
}
